PORT=3001

# Preferred by this project (Prisma runtime connection string)
DATABASE_URL="postgresql://username:password@localhost:5433/database-name?schema=public"

# Optional split variables (for assignment documentation/checklists)
DB_HOST=localhost
DB_PORT=5433
DB_NAME=database-name
DB_USER=username
DB_PASSWORD=password
